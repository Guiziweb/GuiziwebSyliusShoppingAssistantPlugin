<div class="row">
    <div class="col-12 col-md-4 fw-bold">{{ 'guiziweb.ui.session_id'|trans }}</div>
    <div class="col-12 col-md-8">
        {% if hookable_metadata.context.resource.sessionId is not null %}
            <code>{{ hookable_metadata.context.resource.sessionId }}</code>
        {% else %}
            <span class="text-muted">-</span>
        {% endif %}
    </div>
</div>